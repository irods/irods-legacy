#!/bin/sh
#
# Start the iRODS servers.
#
# Usage is:
#	./start
#
IRODS_HOME="/Users/nadeau/Desktop/iRODS/iRODS"
if [ ! -f $IRODS_HOME/bin/runPerlScript ]; then
	echo "Usage error:"
	echo "    Cannot find the necessary 'runPerlScript' file."
	exit 1
fi
$IRODS_HOME/bin/runPerlScript --script $IRODS_HOME/server/bin/start.pl $*
ExitCode="$?"
exit $ExitCode
