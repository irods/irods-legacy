#!/bin/sh
inarg=*A=getErrorStr%*B=$1
text=`irule -v 'msiExecCmd(*A,*B,,,,*C)' $inarg '*C' | grep stdout`
i=1
for text1 in $text ; do
   if [ $i -eq 3 ]
   then
     echo "     $text1"
   fi
   i=`expr $i +  1`
done
exit 0


